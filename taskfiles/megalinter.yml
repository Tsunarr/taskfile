---
version: "3"

tasks:
  default:
    aliases:
      - "test"
    silent: true
    cmds:
      - docker run --pull=always --rm -v "${LOCALWORKSPACE}:$(pwd)" -w
        "$(pwd)" -e DEFAULT_WORKSPACE="$(pwd)" oxsecurity/megalinter:v8
